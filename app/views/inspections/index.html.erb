<div class="container-fluid">
  <ul class="list-group">
    <% @inspections.each do |inspection| %>
      <li><%= link_to inspection.address, client_inspection_path(inspection) %></li>
    <% end %>
  </ul>
</div>
